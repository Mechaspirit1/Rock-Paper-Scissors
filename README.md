# Rock-Paper-Scissors
A little command-line game
# Installation 
* Clone the repository
* run ```chmod +x rps.py```
* and copy or move the script to a PATH directory ```cp rps.py /usr/local/bin``` or ```mv rps.py /usr/local/bin```
